// Worker entry point for the TypeScript and JavaScript language services. See the
// note in `editor.worker.ts` for why these one-line modules exist.
import 'monaco-editor/languages/features/typescript/ts.worker.js';
