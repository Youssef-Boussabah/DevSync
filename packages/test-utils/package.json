{
  "name": "@devsync/test-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable test helpers and fixtures for DevSync.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('@devsync/test-utils: no tests yet (Phase A0).')\"",
    "clean": "node -e \"['.turbo','tsconfig.tsbuildinfo'].forEach((p) => require('fs').rmSync(p, { recursive: true, force: true }))\""
  },
  "devDependencies": {
    "@devsync/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
