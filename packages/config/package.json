{
  "name": "@devsync/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared development configuration for DevSync workspaces.",
  "exports": {
    "./tsconfig.base.json": "./tsconfig.base.json"
  },
  "files": [
    "tsconfig.base.json"
  ],
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('.turbo', { recursive: true, force: true })\""
  }
}
