{
  // What actually ships. The type-check config above also covers the tests, the
  // Prisma CLI config, and the Vitest config; none of those belong in `dist`.
  "extends": "./tsconfig.json",
  "include": ["src"],
  "exclude": ["src/**/*.test.ts", "tests"]
}
