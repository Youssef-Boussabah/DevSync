/**
 * `@devsync/shared`
 *
 * Public surface for the schemas, types, constants, and wire-protocol
 * definitions that both `apps/web` and `apps/api` depend on.
 *
 * This package exists to fix the module boundary early. The implementation is
 * deliberately deferred until the first genuinely shared contract exists;
 * inventing one earlier would be speculation, not design.
 */
export {};
