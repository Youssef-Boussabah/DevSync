/**
 * `@devsync/shared`
 *
 * Public surface for the schemas, types, constants, and wire-protocol
 * definitions that both `apps/web` and `apps/api` depend on.
 *
 * Phase A0 establishes the package boundary only. This module is intentionally
 * empty until the first genuinely shared contract exists.
 */
export {};
