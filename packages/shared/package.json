{
  "name": "@devsync/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared schemas, types, constants, and protocol definitions for DevSync.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('@devsync/shared: no tests yet.')\"",
    "clean": "node -e \"['.turbo','tsconfig.tsbuildinfo'].forEach((p) => require('fs').rmSync(p, { recursive: true, force: true }))\""
  },
  "devDependencies": {
    "@devsync/config": "workspace:*",
    "eslint": "^9.39.5",
    "typescript": "^5.7.3"
  }
}
