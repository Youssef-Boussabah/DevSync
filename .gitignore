# --- Dependencies ---
node_modules/
.pnpm-store/

# --- Build outputs ---
dist/
build/
out/

# Next.js
.next/
.vercel/
next-env.d.ts

# NestJS / TypeScript
*.tsbuildinfo

# Prisma Client, generated into the source tree from prisma/schema.prisma.
# Reproducible from the schema by `pnpm --filter @devsync/database generate`,
# which the build, lint, and type-check tasks all run first.
packages/database/src/generated/

# --- Turborepo ---
.turbo/

# --- Test and coverage outputs ---
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# --- Local environment files ---
# `.env.example` is intentionally tracked; real environment files are not.
.env
.env.*
!.env.example

# --- Logs ---
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Editor and OS metadata ---
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db

# Lockfiles are intentionally NOT ignored: pnpm-lock.yaml is committed.
