# Normalize every text file to LF in the repository and in the working tree, so
# formatting checks behave identically on Windows, macOS, and Linux.
* text=auto eol=lf

# Generated by pnpm — keep it tracked, but out of the way in diffs and language stats.
pnpm-lock.yaml linguist-generated=true
