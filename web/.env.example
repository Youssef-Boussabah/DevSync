# URL of the DevSync server. Required: the editor refuses to connect without it, and code
# execution is proxied through the same server. This is the only execution-related setting
# the browser needs — the JDoodle credentials live in the server environment.
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
