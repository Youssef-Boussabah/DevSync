{
  "name": "@devsync/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser and HTTP smoke tests that exercise the built DevSync applications.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('@devsync/e2e: browser tests are excluded from the default test command; run pnpm test:e2e.')\"",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium",
    "clean": "node -e \"['test-results','playwright-report','blob-report','.turbo','tsconfig.tsbuildinfo'].forEach((p) => require('fs').rmSync(p, { recursive: true, force: true }))\""
  },
  "devDependencies": {
    "@devsync/config": "workspace:*",
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.0.0",
    "dotenv": "^17.4.2",
    "eslint": "^9.39.5",
    "typescript": "^5.7.3"
  }
}
